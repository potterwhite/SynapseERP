# Define the default AppConfig for projects that use this app.
# This allows users to simply add 'synapse_attendance' to their INSTALLED_APPS.
default_app_config = 'synapse_attendance.apps.SynapseAttendanceConfig'
